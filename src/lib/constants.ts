export const ROUTES = {
  login: '/',
  dashboard: '/dashboard',
  storyDetail: '/story/:id',
  settings: '/settings',
} as const

export const TAB_VALUES = {
  LOGIN: 'login',
  REGISTER: 'register',
} as const
